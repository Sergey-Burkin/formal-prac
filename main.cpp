#include<iostream>
#include "CYK.h"
#include "grammar.h"

int main () {
    std::string alpha;
    std::string u;
   // Grammar g;
    std::cout << "Hello!\nEnter n:";
    int n;
    std::cin >> n;
    for (int i = 0; i < n; ++i) {
        std::string rule;
        std::cin >> rule;

    }
    std::cin >> alpha;
    std::cout << "Enter u:";
    std::cin >> u;
    return 0;
}
